# ── Core scraping ─────────────────────────────────────────────────────────────
# fast-flights uses Google Flights' internal protobuf API.
# Pin tightly — upstream API changes can silently break parsing.
fast-flights==0.2.6

# Playwright browser fallback
playwright==1.44.0

# ── Scheduling ────────────────────────────────────────────────────────────────
apscheduler==3.10.4

# ── Display / reporting ───────────────────────────────────────────────────────
rich==13.7.1
tabulate==0.9.0

# ── Date / holiday logic ──────────────────────────────────────────────────────
holidays==0.46
python-dateutil==2.9.0.post0

# ── HTTP / currency ───────────────────────────────────────────────────────────
requests==2.32.3

# ── Environment config (.env support) ────────────────────────────────────────
python-dotenv==1.0.1
